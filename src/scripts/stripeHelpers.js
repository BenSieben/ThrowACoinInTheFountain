/**
 * Reactionary JavaScript to when the submit button
 * is pressed for the Stripe form on the landing page
 * (sends data to Stripe for tokenization)
 */
function tokenizeData() {
    var form = document.getElementById("payment-form");
    document.write(form.toString());
    return false;
}